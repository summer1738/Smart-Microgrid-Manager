# Smart Microgrid Manager — AI / forecasting

Placeholder for:

- Data export from backend DB for training
- LSTM model scripts (generation + consumption)
- Model artifacts (saved .h5 or PyTorch) for deployment

When ready: train on a dev machine, then copy the saved model into the backend or a dedicated inference service for the Pi.
